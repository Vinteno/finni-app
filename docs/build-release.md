# Окружение и сборка релизного APK

Пункт 5.2 ТЗ: требования к окружению, версии инструментов и пошаговая инструкция
сборки. Сторонний специалист должен собрать решение по этому файлу без вопросов.

## Окружение

| Инструмент | Версия |
|---|---|
| {SDK / среда} | |
| JDK | |
| Android SDK | API {26}+ |
| {менеджер пакетов} | |

## Отладочная сборка

```bash
# {шаги}
```

## Релизный APK

Ключ подписи в репозиторий не коммитится (ТЗ 3.4). Хранится у {кто}, путь
задаётся локально в файле, перечисленном в `.gitignore`.

```bash
# {шаги}
```

Результат: `{путь до apk}`

| | |
|---|---|
| Имя пакета | `{PACKAGE}` |
| Версия | `{VERSION}` |
| Номер сборки | `{BUILD}` |
| minSdk | 26 (Android 8.0) |
| targetSdk | {} |

## Демонстрационный режим

{Как включить, что в тестовом профиле, как сбросить.}

## Сброс профиля и тестовые данные

{Шаги.}
